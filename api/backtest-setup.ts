import { backtestSetup } from '../src/utils/backtestEngine.js';
import type { Candle } from '../src/types.js';
type Row=[number,string,string,string,string,string];
const load=async(symbol:string,interval:string,limit:number):Promise<Candle[]>=>{const query=`symbol=${encodeURIComponent(symbol)}&interval=${interval}&limit=${limit}`;let r:Response;try{r=await fetch(`https://fapi.binance.com/fapi/v1/klines?${query}`,{signal:AbortSignal.timeout(5000)});if(!r.ok)throw new Error(`Futures ${r.status}`)}catch{r=await fetch(`https://api.binance.com/api/v3/klines?${query}`,{signal:AbortSignal.timeout(5000)});if(!r.ok)throw new Error(`Spot ${r.status}`)}return(await r.json() as Row[]).map(x=>({timestamp:x[0],timeStr:new Date(x[0]).toISOString(),open:+x[1],high:+x[2],low:+x[3],close:+x[4],volume:+x[5]}));};
export async function GET(req:Request){try{const u=new URL(req.url,'https://bottrade-vercel.local'),symbol=String(u.searchParams.get('symbol')||'BTCUSDT').toUpperCase().replace('/',''),days=Math.min(14,Math.max(1,Number(u.searchParams.get('days')||14))),score=Math.min(90,Math.max(50,Number(u.searchParams.get('score')||65)));const [c15,c1h,c4h]=await Promise.all([load(symbol,'15m',Math.min(1500,days*96)),load(symbol,'1h',Math.min(1500,days*24+200)),load(symbol,'4h',Math.min(500,days*6+200))]);const result=backtestSetup(c15,c1h,c4h,score,32);return new Response(JSON.stringify({symbol,interval:'15m',days,score,...result,source:'Binance USDⓈ-M Futures • Spot fallback • Confluence Backtest'}),{headers:{'content-type':'application/json','cache-control':'s-maxage=60, stale-while-revalidate=120'}})}catch(error){return new Response(JSON.stringify({error:error instanceof Error?error.message:'Backtest unavailable'}),{status:502,headers:{'content-type':'application/json'}})}}
